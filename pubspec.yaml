name: afterlife_projects
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  shared_preferences: ^2.2.2
  firebase_core: ^3.9.0
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.6.0 
  provider: ^6.1.2
  confetti: ^0.8.0
  intl: ^0.18.0
  sensors_plus: ^6.1.1
  http: ^1.2.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.0
  flutter_map_marker_cluster: ^1.3.2
  url_launcher: ^6.3.0
  qr_flutter: ^4.1.0
  local_auth: ^3.0.1
  share_plus: ^13.1.0
  screenshot: ^3.0.0
  pdf: ^3.12.0
  geolocator: ^14.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/imatges/logo_afterlife.png
    - assets/imatges/palmeras.png
